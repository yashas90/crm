[build]
builder = "nixpacks"
buildCommand = "pnpm install --prod=false && pnpm railway:build"

[deploy]
startCommand = "pnpm railway:start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
