NEXT_PUBLIC_API_URL=http://localhost:3001
# Optional — Sentry DSN for web error tracking (leave unset to disable).
# Set SENTRY_DSN_WEB in Vercel; it is exposed to the browser as NEXT_PUBLIC_SENTRY_DSN_WEB at build time.
# SENTRY_DSN_WEB=https://examplePublicKey@o0.ingest.sentry.io/0
